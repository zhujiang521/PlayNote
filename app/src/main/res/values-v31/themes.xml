<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.PlayNote" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- 设置状态栏透明 -->
        <item name="android:statusBarColor">@color/background</item>
        <!-- 设置导航栏透明 -->
        <item name="android:navigationBarColor">@color/background</item>
        <!-- 使内容延伸到状态栏 -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <!-- 使内容延伸到导航栏 -->
        <item name="android:fitsSystemWindows">false</item>
        <!--使用 windowSplashScreenBackground 以特定的单色填充背景-->
        <item name="android:windowSplashScreenBackground">@color/background</item>
        <!--使用 windowSplashScreenAnimatedIcon 替换起始窗口中心的图标。-->
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_ai_avd</item>
    </style>

</resources>