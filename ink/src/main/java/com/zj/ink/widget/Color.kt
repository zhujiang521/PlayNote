package com.zj.ink.widget

import androidx.compose.ui.graphics.Color
import androidx.glance.color.ColorProvider


val textColor = ColorProvider(
    day = Color(25, 25, 25),
    night = Color(250, 250, 250)
)

val gray =  ColorProvider(
    day = Color.LightGray,
    night = Color.Gray
)
